config:
  target: 'https://api.howdju.com'
  phases:
    - duration: 60
      arrivalRate: 5
  defaults:
    headers:
scenarios:
  - name: "Read statement justifications"
    flow:
    - get:
        url: "/api/statements/1525?include=justifications"
  - name: "Search"
    flow:
      - get:
          url: "/api/search?searchText=trump"
