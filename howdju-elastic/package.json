{
  "name": "howdju-elastic",
  "version": "1.0.0",
  "description": "Artifacts and scripts related to running Elasticsearch in Docker",
  "main": "index.js",
  "author": "Carl Gieringer <carl.gieringer@gmail.com>",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "argparse": "^1.0.10",
    "bluebird": "^3.5.2",
    "eslint": "^7.25.0",
    "ini": "^1.3.5",
    "prettier": "2.7.1"
  },
  "scripts": {
    "check-format": "yarn run prettier --check --ignore-path .gitignore .",
    "format": "yarn run lint --fix && yarn run prettier --write --ignore-path .gitignore .",
    "deploy-elasticsearch-image": "node bin/deploy-elasticsearch-image.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "debug": "^4.3.4"
  }
}
